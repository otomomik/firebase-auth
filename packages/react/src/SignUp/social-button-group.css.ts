import { style } from '@vanilla-extract/css'

export const socialButtonGroup = style({
  display: 'flex',
  flexDirection: 'column',
  gap: 16,
  width: '100%',
  paddingLeft: 16,
  paddingRight: 16,
})
